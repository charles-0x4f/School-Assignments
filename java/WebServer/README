A very simple web server that listens for a connection and sends the requested URL-specified file if it receives a GET command

Note: This was shoved into one file, this should be put into its own class to stay with Java conventions, will clean when time permits
